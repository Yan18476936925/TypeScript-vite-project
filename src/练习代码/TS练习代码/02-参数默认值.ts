export {};
const handler = (a = 1, b = 2) => {
  return a + b;
};

handler(10, 20);
console.log('handler(10,20)----->', handler(10, 20));

let age = 18;
// age = 20;
// age = "30";
age.toFixed();
[].every;
Math.random;
